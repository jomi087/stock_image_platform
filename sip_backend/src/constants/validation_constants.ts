export const PASSWORD_MIN_LENGTH = 8;
export const STRONG_PASSWORD_REGEX = /^(?=.*[A-Z])(?=.*\d).{8,}$/;
export const EMAIL_REGEX = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
export const MAX_CAP_4_ALL_IMAGE = 10 * 1024 * 1024;
export const MAX_CAP_IMAGE = 5 * 1024 * 1024;
